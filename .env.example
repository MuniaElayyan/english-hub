# Copy this file to ".env" and fill both values to publish the teacher's
# uploads to everyone (Supabase → Project Settings → API).
# Leave them empty and the site keeps uploads on the teacher's own device only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
